name: Crypto Cron

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Ping run endpoint
        run: |
          curl -s "${{ secrets.RENDER_URL }}/run?key=${{ secrets.RUN_KEY }}" || true